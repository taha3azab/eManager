@model IEnumerable<eManager.Domains.Department>

@{
    ViewBag.Title = "All Departments";
}

<h2>@ViewBag.Title</h2>

<ul>
    @foreach (var department in Model)
    {
        <li>
            @department.Name
        </li>
    }
</ul>